<h2>Posts</h2>

<div class="row">
  {{#each post in model}}
    <article class="col-sm-4">
      <h4>{{#link-to 'posts.show' post}}{{post.title}}{{/link-to}}</h4>
      <p>{{post.body}}</p>
    </article>
  {{/each}}
</div>
